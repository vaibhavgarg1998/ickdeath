# Public (safe in the browser)
# WhatsApp Business number with country code, digits only — e.g. 9198XXXXXXXX
NEXT_PUBLIC_WHATSAPP_NUMBER=

# Optional: Google Apps Script / Sheet webhook to store orders (Phase 1)
NEXT_PUBLIC_ORDERS_WEBHOOK_URL=

# Product price is in src/lib/product.ts (unitPricePaise)
