rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /orders/{orderId} {
      // Guest checkout can create pending orders from the website.
      allow create: if request.resource.data.keys().hasAll([
          'orderId', 'createdAt', 'contact', 'address',
          'quantity', 'amountPaise', 'paymentMethod',
          'paymentStatus', 'orderStatus'
        ])
        && request.resource.data.orderId == orderId
        && request.resource.data.paymentStatus == 'pending'
        && request.resource.data.orderStatus == 'confirmed'
        && request.resource.data.paymentMethod in ['razorpay', 'whatsapp'];

      // Only signed-in admin users can list / read / update.
      // Payment "paid" updates should use Firebase Admin from the verify API.
      allow read, update: if request.auth != null;

      allow delete: if false;
    }
  }
}
